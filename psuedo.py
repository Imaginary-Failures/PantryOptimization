# Create and sort a list of Foodstuff objects based on length * depth


# determine how things are getting added to the stack
# While shelf != empty:
    # while currStack.height < currStack.max_height:
        #if
        #currStack.push


